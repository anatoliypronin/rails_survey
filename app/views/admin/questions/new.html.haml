= simple_form_for @question, url: admin_survey_questions_path(survey: @survey.id) do |f|
  = f.input :title
  = f.input :position
  = f.select :kind, Question.kind.options
  = f.button :submit, class: 'btn btn-info'
