= simple_form_for @variant, url: admin_variants_path do |f|
  = f.association :question, include_blank: false
  = f.input :title
  = f.button :submit, class: 'btn btn-info'
= link_to t('.back'), admin_variants_path, class: 'btn btn-info'
