= simple_form_for @variant, url: admin_variant_path(@variant.id) do |f|
  = f.input :title
  = f.button :submit, class: 'btn btn-info'
= link_to t('.back'), admin_variant_path(@variant), class: 'btn btn-info'
