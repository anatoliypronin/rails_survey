= simple_form_for @question, url: respondent_survey_questions_path do |f|
  = f.input :title
  = f.input :position
  = f.select :kind, Question.kind.options
  = f.button :submit, class: 'btn btn-info'

